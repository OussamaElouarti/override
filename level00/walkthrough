# level00 walkthrough
#

* The binary reads an integer from input and compares that integer with the literal 0x149c, if
the comparison is true it executes /bin/sh with system, if not it prints "Invalid Password" and returns.
